import { MaxMinLengthDirective } from './max-min-length.directive';

describe('MaxMinLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxMinLengthDirective();
    expect(directive).toBeTruthy();
  });
});
