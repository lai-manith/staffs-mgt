import { TableStatusPipe } from './table-status.pipe';

describe('TableStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TableStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
