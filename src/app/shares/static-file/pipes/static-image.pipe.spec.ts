import { StaticImagePipe } from './static-image.pipe';

describe('StaticImagePipe', () => {
  it('create an instance', () => {
    const pipe = new StaticImagePipe();
    expect(pipe).toBeTruthy();
  });
});
